.page-chatMessage
  .chatMessage_ct
    width: 100%
    .v-list__tile
      .dataline
        margin-left: 8px
        $font-size(10px)
        color: $text-disabled
      &__title
        $font-size(14px)
      &__sub-title
        height: 2em
        $font-size(12px)
        line-height: 2em
        overflow: hidden
      &__action
        .v-icon
          $font-size(20px)
        &-text
          $font-size(10px)